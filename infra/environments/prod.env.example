APP_ENV=prod
# Supply DATABASE_URL=postgresql+psycopg://... from a secret manager
CORS_ORIGINS=["https://app.example.invalid"]
PUBLIC_APP_URL=https://app.example.invalid
SMTP_HOST=
SMTP_FROM=
SMTP_USERNAME=
SMTP_PASSWORD=
